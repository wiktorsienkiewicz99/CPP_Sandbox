#include <iostream>
#include <vector>
#include "OOPPillars/Pillar.h"
import VectorModule;
//#include "Collections/Vector.h"

int main() {
    /*
    //Encapsulation
    EncapsulationGameCharacter ch1(200);
    ch1.printHealth();
    ch1.heal(200);
    ch1.printHealth();
    ch1.dealDamage(150);
    ch1.printHealth();

    //Abstraction
    Player player("Hero");
    Goblin goblin;
    performBattle(&player, &goblin);   // Hero attacks Goblin
    performBattle(&goblin, &player);   // Goblin attacks Hero

    //Polymorphism
    Door door;
    Chest chest;
    playerInteractsWith(&door);
    playerInteractsWith(&chest);
    */

    runVectorModule();

    return 0;
}
